// Wood shader using a texture map
shader wood(
    color Cin=1,             // Input color (not used in this shader)
    output  color Cout=0     // Output color
)
{
    // Sample the wood texture at the given (u, v) coordinates and set the output color
    Cout=(color)texture("wood.tex", u, v);
}
